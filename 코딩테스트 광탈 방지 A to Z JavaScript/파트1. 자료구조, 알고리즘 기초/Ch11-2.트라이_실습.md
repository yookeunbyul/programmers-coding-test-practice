```
class Node {
    constructor(value = ""){
        this.value = value;
        this.children = new Map();
    }
}

class Trie {
    constructor() {
        //루트는 빈 노드로
        this.root = new Node();
    }

    insert(string){
        //루트로 시작
        let currentNode = this.root;
        for(const char of string){
            //만약 자식에 이 단어가 없다면
            if(!currentNode.children.has(char)){
                //단어를 key(간선)로 정점(노드)에는 문자의 수만 넣어준다
                currentNode.children.set(char, new Node(1));
            }else{
                const children = current.children.get(char);
                //노드에 있는 value값 증가
                children.value++;
                currentNode.children.set(char, children);
            }
            //다음 노드로 이동
            currentNode = currentNode.children.get(char);
        }
    }

    has(string){
        let currentNode = this.root;

        for(const char of string){
            if(!currentNode.children.has(char)){
                return false;
            }
            currentNode = currentNode.children.get(char);
        }
        return true;
    }
 }

function solution(words) {
    var answer = 0;
    return answer;
}
```
